ALGORITMO: area_circulo
    DECLARE
        area, raio :NUMERICO

    ESCREVA("Digite o valor do raio: )
    LEIA("raio)

    area = 3.14 * (raio * raio)

    ESCREVA("A area do circulo é:", area)
FIM_DO_ALGORITMO